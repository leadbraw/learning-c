Introduction to pointers, pointer arithmetic, manipulation, et cetera. Also of course touched on
arrays and strings. Basic exercises: making my own `swap()` function, triggering a segfault on purpose, 
and some other stuff. The project was to make a little string library `mystr`, implementing my own
versions of `strlen`, `strcpy`, `strcmp`, `strrev`, and an improved version of `strtok`.
