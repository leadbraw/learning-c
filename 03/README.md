Learned makefiles, redid an exercise from module 1 to include a header file `greet.h` and wrote a
Makefile for it. inside ./stats/ is  a little statistics tools. It reads numbers from stdin and
reports the min, max, mean, stddev, and median (optionally, with a command line argument).
